# Local development — Vite proxy handles /api → localhost:5000
# VITE_API_URL is NOT set here so Vite proxy is used automatically
# On Render, set VITE_API_URL=https://your-backend.onrender.com/api in the dashboard
